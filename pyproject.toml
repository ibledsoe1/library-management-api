[project]
name = "library-management-api"
version = "0.1.0"
description = "An API for managing a library. This includes it's members and books."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.0",
]
